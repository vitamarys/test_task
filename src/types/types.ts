export interface ILogin {
  email: string;
  password: string;
}
export interface IToken {
  access_token: string;
}
export interface IProduct {
  body: string;
  created_at: string;
  id: number;
  price: string;
  thumbnail: string;
  title: string;
  updated_at: string;
}
